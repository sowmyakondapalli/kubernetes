apiVersion: v1
kind: Pod
metadata:
  name: nodeport-Service
  labels:
    name: my-app
    environment: dev
spec:
  containers:
    - name: nginxcont
      image: nginx
      port:
      - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: np-Service
spec:
  type: nodePort
  selector:
    name: my-app
    environment: dev
  ports:
    - protocol: 
      port: 80
      targetPort: 80
      nodePort: 30080 

    
